/* Host shim.  map_tiles.c only touches BSP_SD_MOUNT_POINT out of the real
   BSP header, so keep this deliberately empty of anything else. */
#ifndef LS_SHIM_BSP_ESP_BSP_H
#define LS_SHIM_BSP_ESP_BSP_H
#define BSP_SD_MOUNT_POINT "/sdcard"
#endif
